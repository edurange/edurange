In order to run on same machine as edurange-server:

Apt install node (if you don’t have it already)
git clone https://github.com/stuicey/wsProxy.git
Cd wsProxy
Npm install
Node index.js

(something like that)