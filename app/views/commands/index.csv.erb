<% require 'csv' %>
<%= raw(CSV.generate(headers: true) do |csv|
  @query.command_history.reverse_each do |record|
    csv << [
      record.begin,
      record.instance.name,
      record.time,
      record.cwd,
      record.command,
      record.output,
      record.prompt
    ]
  end
end) %>
