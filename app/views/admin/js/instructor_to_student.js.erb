$('#user-<%= user.id %>').remove();
$('#students-table > tbody').append('<%= escape_javascript(render("/admin/student", user: user)) %>');
$('#student-group-<%= student_group.id %>-table > tbody').append(
    '<%= escape_javascript(render("/student_groups/student", student_group: student_group, user: user)) %>');
