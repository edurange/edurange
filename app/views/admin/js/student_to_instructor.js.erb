$('#user-<%= current_user.id %>').remove();
$('.student-group-user-<%= current_user.id %>').each(function(i, obj) {
    $(obj).remove();
});
$('#instructors-table').append('<%= escape_javascript(render("/admin/instructor", user: current_user)) %>');
