!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Edurange'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Edurange'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
    = yield(:head)
  %body
    %br/
    %br/
    %main{:role => "main"}
      %div.col-md-8.col-md-offset-2
        = render 'layouts/messages'
      %div.card
        %div.card-header
          %h1
            = link_to image_tag("https://edurange.org/img/logo_menu.png")
          %h4.font-weight-light
            the cybersecurity training ground
          %hr
          %h4.font-weight-normal
            Visit our website for more information - <a href="http://edurange.org">edurange.org</a>
        %div.card-body
          = yield
