%label Roles:
- if @scenario.modifiable?
  %div.inline-block
    .dropdown{id:'dropdown-roles'}
      %button#dropdownMenuButton.btn.btn-secondary.dropdown-toggle{type:'button', id:'dropdown-button-roles', "data-toggle" => 'dropdown', "aria-expanded" => "true"}
        %span.glyphicon.glyphicon-pencil{"aria-hidden" => "true"}
        %span.caret
      .dropdown-menu{role: 'menu', "aria-labelledby" => 'dropdown-button-roles'}
        .dropdown-item{ id: 'role-add-button-li' }
          = link_to 'Add Role', "#modal-role-add", :data => {:toggle=>"modal"}, onclick: "$('#modal-role-add').find('#name').val(''); $('#modal-role-add').find('#name-div').removeClass('has-error'); $('#modal-role-add').find('#name-help').html('');", id: 'role-add-button'
%br
%br
%ul#roles
  - @scenario.roles.each do |role|
    = render '/scenarios/show/role', role: role
